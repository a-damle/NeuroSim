METHODOLOGY CHANGES FROM PROPOSAL
===================================

This document tracks all changes from the original proposal for reference
when writing the final paper.


1. ALGORITHM CHANGE: IRGM → Unpreconditioned GMRES
----------------------------------------------------
Proposal: LU-preconditioned GMRES (IRGM) from Haidar et al.
Final:    Unpreconditioned GMRES with diagonal preconditioner from Le Gallo et al.

Reason: CIM arrays are weight-stationary — one operand is programmed into the
memory array and stays fixed. In IRGM, the LU factorization requires updating
the array at every factorization step (O(n/nb) reprogramming cycles), which is
expensive and impractical on real hardware. Le Gallo's approach only requires
programming A once (or A_tilde = M^{-1}A - I once), and all subsequent
operations are reads.

Reference: Le Gallo et al., "Mixed-Precision In-Memory Computing," 2018,
Algorithm 2 (GMRES variant).


2. PRECONDITIONING STRATEGY
-----------------------------
Proposal: Did not discuss preconditioning in detail.
Final:    Two modes depending on matrix type:
  - Symmetric matrices (types 1, 2, 3, 5): Diagonal preconditioner M = diag(A).
    CIM array stores A_tilde = M^{-1}A - I. The identity component is added
    digitally to avoid CIM noise on the dominant diagonal entries.
  - Non-symmetric matrices (types 4, 6): No preconditioner (M = I).
    CIM array stores A directly. Diagonal preconditioner fails because diag(A)
    can have near-zero entries for non-symmetric matrices (A = UΣVᵀ with U ≠ V).

This distinction affects what is physically programmed into the CIM array.


3. NON-SYMMETRIC MATRICES (TYPES 4, 6)
-----------------------------------------
Proposal: Evaluate all 6 matrix types from Haidar et al. Table II.
Final:    Types 4 and 6 are included as FP64 baselines only, not in the
          quantized/CIM sweeps.

Reason: Without LU preconditioning, unpreconditioned GMRES on non-symmetric
matrices requires inner_iters ≈ n (full GMRES budget) to converge. At n=500,
this means 500 CIM matmuls per outer iteration — eliminating the efficiency
advantage of CIM. This is discussed as a limitation and motivation for
future work on CIM-compatible preconditioners (block diagonal, polynomial).


4. MATRIX SIZE
---------------
Proposal: 4K × 4K matrices.
Final:    500 × 500 matrices.

Reason: Simulation overhead. NeuroSim runs are expensive and the quantization
sweep involves hundreds of configurations. Convergence behavior (which
precisions converge, which don't) is expected to be similar at both sizes.


5. TEST MATRICES
-----------------
Proposal: Same 6 synthetic matrix types from Haidar et al. Table II.
Final:    Same, but note that these are harder than what Le Gallo tested.
          Le Gallo used well-conditioned covariance matrices where the diagonal
          preconditioner works very well. Using Haidar's matrices probes the
          limits of the approach beyond the original Le Gallo paper.

Justification for paper: Haidar's matrices span a wider range of difficulty
and applications than Le Gallo's covariance matrices.


6. EXPERIMENT 0: INDEPENDENT PRECISION SWEEP
----------------------------------------------
Proposal: Sweep integer bit-width from 16 to 4 bits (single parameter).
Final:    Sweep weight_bits and input_bits independently, each from
          {4, 6, 8, 10, 12, 16}. This matches the independent weight/input
          precision parameters in Table I and reveals whether the precision
          floor is driven more by the weight operand (stationary in array)
          or the input operand (streamed through).


7. QUANTIZATION SCHEME
------------------------
Proposal: Did not specify quantization details.
Final:    Matches NeuroSim's TensorQuantizer exactly:
  - Signed symmetric, narrow range (e.g., [-127, 127] for 8-bit)
  - Weight operand (A_tilde): per-row quantization (one scale per row)
  - Input operand (v):        per-tensor quantization (one scale for all)


8. CIM MATMUL AS PLUGGABLE FUNCTION
--------------------------------------
Proposal: Replace low-precision computations with simulated ACIM.
Final:    Solver accepts a pluggable matmul_fn(W, v) → w. Three variants:
  - Experiment 0: acim_matmul (quantization noise only, no analog effects)
  - Experiment 1: NeuroSim CIMLinear (quantization + analog noise)
  - Experiment 2: Same as Exp 1, with PPA traces for energy estimation
  This design means the same solver code is used for all three experiments.


9. INNER GMRES ITERATIONS (m)
-------------------------------
Proposal: Did not specify.
Final:    m = 30 for symmetric types (1, 2, 3, 5) with diagonal preconditioner.
          m = 500 (= n) for non-symmetric types (4, 6) without preconditioner.

Le Gallo used m = 5 for n = 500 and m = 10 for n = 5000, but their matrices
were much easier (well-conditioned covariance matrices).


10. CONVERGENCE TOLERANCE
---------------------------
Proposal: "Until the residual is sufficiently small."
Final:    tol = 1e-12 on ||r||_2, max 200 outer iterations.


11. NEUROSIM BUGS FIXED
--------------------------
Three bugs in NeuroSim v1.5-dev hook.py (PPA trace generation):
  1. self.batch_size → self._cim_args.batch_size
  2. self.name → self._cim_args.name
  3. dec2val called with 3 args instead of 5

Additional issue: hook.py pre-encoded data (dec2bin/dec2val) but the v1.5 C++
backend does its own encoding. Fixed to write raw floats for weights and raw
integers for inputs. Input orientation also fixed (needs one row per input
element, not one row per vector).


12. EXPERIMENTS 1-2: CIM SWEEP CONFIGURATIONS
------------------------------------------------
Proposal: Sweep {6, 8, 10} × {6, 8, 10} × {32, 64, 128} for all matrix types.
Final:    Sweep ranges vary per matrix type, selected based on experiment 0 results.

Methodology for selecting configs: For each matrix type, we identified the
smallest contiguous block of (weight_bits, input_bits) combinations where
every pair in the block converged under ideal quantization (no analog noise).
Configurations that diverged at ideal quantization will only perform worse
with additional analog noise from CIM, so they are excluded.

  Type 1 (diag dominant):      w ∈ {4, 6, 8},  i ∈ {4, 6, 8}    → 9 combos
  Type 2 (log-uniform σ, +λ):  w ∈ {12, 16},   i ∈ {10, 12, 16}  → 6 combos
  Type 3 (clustered σ, +λ):    w ∈ {10, 12, 16}, i ∈ {10, 12, 16} → 9 combos
  Type 5 (arithmetic σ, +λ):   w ∈ {8, 10, 12}, i ∈ {10, 12, 16}  → 9 combos
  Types 4, 6 (non-symmetric):  excluded (see item 3)

Note: Type 1 uses precisions BELOW the original Table I range ({6, 8, 10})
because experiment 0 showed convergence down to 4-bit — this is extended
to capture the full viable range for diag-dominant matrices.

Each precision combo is crossed with:
  - Row parallelism: {32, 64, 128} (from Table I)
  - Device type: SRAM, RRAM (from Table I)

Total: 33 precision combos × 3 parallelism × 2 devices = 198 NeuroSim PPA runs.

However, row parallelism and device type affect both PPA and analog noise.
With realistic noise parameters enabled (item 14), different row parallelism
values may produce different convergence behavior (more parallel rows →
more accumulated noise per ADC read). Each (precision, parallelism, device)
combination requires its own solver run.

Experiments 1 and 2 are the same experiment presented two ways:
  - Experiment 1: convergence curves (residual vs iteration)
  - Experiment 2: Pareto frontier (total CIM energy vs total iterations)
Total CIM energy = (outer_iters × inner_iters) × energy_per_matmul.


13. NEUROSIM BUG: write_layer NOT IMPORTED IN macro.py
--------------------------------------------------------
In addition to the hook.py bugs (item 11), macro.py calls write_layer()
on line 26 of simulate_array but never imports it. Fix: add
    from pytorch_quantization.utils.hook import write_layer
to the top of macro.py. This is the fifth NeuroSim v1.5-dev bug found.


14. ACIM DEVICE PARAMETERS AND NOISE MODELING
------------------------------------------------
Proposal: Used NeuroSim default configurations as representative baselines.
Final:    RRAM only. SRAM dropped (see item 17). Device parameters from
          published silicon measurements. Uses NeuroSim's full analog
          simulation path (output_noise=0.0) with physics-based noise.

CRITICAL NEUROSIM FINDING: NeuroSim's ADC_output() has two mutually
exclusive code paths:
  - output_noise > 0: DIGITAL SHORTCUT. Does integer matmul + flat Gaussian
    noise. Bypasses ALL device physics (conductance, read noise, D2D, drift).
  - output_noise == 0: FULL ANALOG PATH. Conductance lookup, D2D variation,
    read noise, V=IG computation, amplifier feedback, ADC sensing.

Setting output_noise > 0 causes read_noise and mem_states D2D parameters
to have ZERO EFFECT. Previous sweep results used output_noise=0.28 and
were therefore running the digital shortcut, not the analog simulation.

Fix: Set output_noise=0.0 to enable the full analog path. ADC quantization
noise emerges naturally from the sensing model (amplifier feedback +
voltage step + ADC rounding) rather than needing explicit injection.

RRAM Parameters (Intel 22nm FinFET ReRAM):
  Device:  Ron = 3kΩ (G_LRS = 3.33e-4 S), Roff = 40kΩ (G_HRS = 2.5e-5 S),
           on/off ratio ≈ 13, 1-bit/cell.
  Source:  Read et al., "NeuroSim V1.5," 2026, Table I.

  (1) Read noise (σ_read = 0.05, relative fraction of conductance per read):
      Applied as conductance * (1 + N(0, 0.05)) per read cycle in the analog
      path. Noise scales with number of activated rows (physically correct:
      more bitline currents summing = more accumulated noise).
      Source: Chen et al., IEEE TCAD 2018.

  (2) D2D variation (stored in mem_states_rram.csv):
      LRS: mean = 3.33e-4 S, std = 3.33e-5 S (10% of G_LRS)
      HRS: mean = 2.5e-5 S,  std = 5.0e-6 S  (20% of G_HRS)
      Programmed once at layer creation (deterministic across matmuls).
      Source: Read et al. 2026, Fig. 6.

  (3) ADC output noise: NOT explicitly set (output_noise=0.0).
      ADC quantization noise emerges from the analog sensing model:
        - Amplifier feedback scales as 1/(p × ΔG)
        - ADC step = V_dd × ΔG × amp_feedback
        - Quantization via round() + clamp to ADC range
      This is more physically accurate than flat Gaussian injection.


15. RRAM DEVICE PARAMETERS IN cim_args
-----------------------------------------
Proposal: Used NeuroSim defaults (off_state=6e-3, on_state=6e-3*17, on/off=17).
Final:    Updated to Intel 22nm values (off_state=2.5e-5, on_state=3.33e-4,
          on/off≈13) to match the device characterization in Read et al. 2026.


16. NEUROSIM BUGS #6 AND #7 (macro.py)
-----------------------------------------
Bug #6 (conductance clamp): conductance_sampling can produce negative values
via Gaussian sampling (physically impossible). read_noise then gets negative
std, crashing torch.normal. Fix: clamp conductances to min=0 after sampling.
  memory_states = torch.clamp(memory_states, min=0.0)
  memory_states_ref = torch.clamp(memory_states_ref, min=0.0)

Bug #7 (output_noise bypass): When output_noise > 0, ADC_output returns
early at line 196, bypassing the entire analog simulation (conductance,
read noise, D2D, drift). This is by design (two separate models) but
is not documented. Users who set both output_noise and read_noise get
only the digital shortcut — read_noise has zero effect.

Total NeuroSim v1.5-dev bugs found: 7 (5 in hook.py, 2 in macro.py).


17. SRAM DROPPED FROM STUDY
------------------------------
Proposal: Compare SRAM and RRAM CIM devices.
Final:    SRAM dropped. NeuroSim v1.5's analog simulation path models
          resistive memory physics (I=VG, conductance-based sensing).
          SRAM CIM operates via different mechanisms (charge-domain Q=CV
          or current-domain transistor gating) that this model does not
          capture. The NeuroSim authors acknowledge this limitation:
          "Regarding 6T SRAM mode... we have not fully incorporated more
          intricate simulations to account for the unique non-idealities
          found in analog SRAM mode" (Read et al. 2026).

          Previous SRAM results used the digital shortcut (output_noise=0.26)
          which bypassed the conductance model entirely. While those results
          showed SRAM's lower noise floor, the modeling approach lacks
          physical grounding. SRAM CIM comparison is noted as future work
          pending improved simulator support.


18. REVISED CIM SWEEP (FINAL)
--------------------------------
Previous sweep: 4 types × 33 precision × 3 parallelism × 3 K = 297 configs
Final sweep:    2 types × 9 precision × 3 parallelism × 3 K = 162 configs

Only types 1 and 3 showed any convergence in previous runs:
  Type 1 (diag dominant): all configs converge
  Type 3 (clustered σ, +λ): converges at high precision + high K

Types 2 and 5 did not converge at any configuration even with K=8,
and are excluded from the final sweep. Their non-convergence is
reported as a finding (analog noise too severe for these condition
numbers with diagonal preconditioning).

  Type 1: w∈{4,6,8}, i∈{4,6,8} × p∈{32,64,128} × K∈{1,4,8} = 81 configs
  Type 3: w∈{10,12,16}, i∈{10,12,16} × p∈{32,64,128} × K∈{1,4,8} = 81 configs


16. DEVICE AVERAGING (K PARAMETER) — IMPLEMENTED
---------------------------------------------------
Proposal: Not discussed.
Final:    Implemented Le Gallo's K-device averaging. Each matrix element is
          stored in K devices and the read results are averaged, reducing
          effective noise by approximately 1/√K across all noise sources.

K values swept: {1, 4, 8}

Implementation: K sequential forward passes through CIMLinear, outputs
averaged. Tiled approach (stacking K copies of W along output dimension
for single-pass execution) was tested but rejected — the shared reference
column in simulate_array causes correlated noise across tiles, defeating
the purpose of averaging.

PPA scaling for K devices read in parallel:
  - Energy: K × single-device energy (K independent column groups reading)
  - Latency: unchanged (parallel read)
  - Area: K × single-device area

Key finding: K averaging significantly improves convergence for moderate
matrices (Type 3: residual drops from 1e-1 to 1e-9 going K=1→K=8) but
cannot rescue the hardest matrices (Types 2, 5) where noise is too severe.


17. FP16 DIGITAL BASELINE
----------------------------
Proposal: Mentioned FP16 baseline per Prof. Li's suggestion but no details.
Final:    FP16 baseline implemented as direct torch.float16 casting of both
          operands in the inner loop matmul. This matches what hardware FP16
          units do — NOT the same as 16-bit integer quantization via acim_matmul.

          FP16 (IEEE 754 half-precision): 5-bit exponent, 10-bit mantissa,
          non-uniform spacing, dynamic range ±65,504.
          INT16 (acim_matmul at 16-bit): uniform integer grid, no exponent.

          On CUDA with tensor cores (RTX 3080 Ti), PyTorch accumulates FP16
          matmuls in FP32 internally, matching Haidar's FP16-TC path — the
          most relevant modern hardware comparison.

Cost model: PETRA (Cho et al., VLSI Circuits 2021), 22nm Intel FinFET.
  - Same process node as NeuroSim RRAM/SRAM configs.
  - Measured silicon, FP16 native, 1024 MACs.
  - Two operating points:
      Nominal (0.88V): 0.89 pJ/MAC, 701 MHz → 222 nJ/MVM, 0.348 µs/MVM
      Peak eff (0.70V): 0.29 pJ/MAC, 416 MHz → 71.7 nJ/MVM, 0.587 µs/MVM
  - Area: 3.04 mm²
  - 500×500 MVM = 250,000 MACs

Total solver cost = outer_iters × inner_iters × cost_per_MVM.


18. DATA FILES AND FORMATS
-----------------------------
Three .pt result files with different key formats (all loadable with just
PyTorch, no NeuroSim needed):

  exp0_results_light.pt:
    Key: (mtype, wb, ib)
    Fields: history, iters, final_residual, rel_error, converged
    Baselines: FP64 per matrix type (history and iters only, no A/x_ref)
    Matrices regenerated deterministically via TestMatrixGenerator(500, seed=42)

  exp12_{device}_results.pt:
    Key: (mtype, wb, ib, pr, K)
    Fields: history, iters, final_residual, rel_error, converged,
            stop_reason, total_matmuls, K, ppa_single, total_energy_pJ,
            total_latency_ns, total_area_um2
    Baselines: FP64 per matrix type

  fp16_baseline_results.pt:
    Key: mtype (int)
    Fields: history, iters, final_residual, rel_error, converged,
            stop_reason, total_matmuls, fp64_history, fp64_iters,
            petra_costs (per PETRA operating point: energy, latency, area)

All files contain full convergence histories (residual per outer iteration)
and can be re-plotted at any convergence threshold without re-running.


19. POTENTIAL FUTURE DIRECTIONS
---------------------------------
- CIM-compatible preconditioners: block diagonal or polynomial preconditioners
  as middle ground between weak diagonal and expensive LU.
- Larger K values (K=16, 32) for harder matrix types at the cost of
  proportionally more area and energy.
- Larger matrix sizes (4K×4K as in original proposal) to verify convergence
  behavior scales.
- Non-symmetric matrices with LU preconditioning: quantify the reprogramming
  cost and compare against the unpreconditioned approach.
